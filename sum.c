#include <stdio.h>

int sumFinder(int num1, int num2) {
    int sum = num1 + num2;
    printf("%d", sum);
}

int diff() {
    printf("vrfskdml");
}

int main() {
    int num1, num2;
    scanf("%d %d", &num1, &num2);
    sumFinder(num1, num2);
    diff();
    return 0;
}